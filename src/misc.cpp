#pragma once

#include <torch/torch.h>
#include <iostream>

//std::ostream& operator<<(std::ostream& os, const torch::Tensor& tensor) {
//    // Print the sizes of the tensor
//    os << "Tensor Sizes: " << tensor.sizes() << std::endl;
//
//    // Print the dtype of the tensor
//    os << "Tensor Dtype: " << tensor.dtype() << std::endl;
//
//    return os;
//}
